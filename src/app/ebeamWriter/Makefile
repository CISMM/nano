USES_LIBS := ImgFormat nmReg nmImageViewer nmBase vrpn quat tclLinkVar

ifdef VC
  USES_EXTERNAL_LIBS := tcl82 tk82
else
  USES_EXTERNAL_LIBS := tcl tk
endif 

APP_NAME=seegerizer

include ../Makefile.share
