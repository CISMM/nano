The program corescribe is a simple utility that takes a list of cores
from coreview ("core"), along with an image in pgm or usr/Image
format, and produces a new image with the cores superimposed on it.
There are tick marks at default ten-pixel intervals.
